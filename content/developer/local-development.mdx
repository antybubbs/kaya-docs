---
title: "Local Development"
description: "Run Kaya Docs while developing."
order: 4
version: "current"
---

Install dependencies and start the development server:

```bash
npm install
npm run dev
```

Open `http://localhost:3000`.

Set `DOCS_AUTH_USERS` and `DOCS_AUTH_SECRET` in your environment when testing editor authentication.
